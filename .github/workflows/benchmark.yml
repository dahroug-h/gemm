name: Benchmark Matmul (AVX2-only, Industry Standard)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  benchmark:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Cache apt packages
        uses: actions/cache@v4
        with:
          path: /var/cache/apt/archives
          key: apt-${{ runner.os }}-${{ hashFiles('.github/workflows/benchmark.yml') }}
          restore-keys: apt-${{ runner.os }}-

      - name: Cache Intel MKL
        id: cache-mkl
        uses: actions/cache@v4
        with:
          path: /opt/intel/oneapi
          key: mkl-oneapi-ubuntu22-2025
          restore-keys: mkl-oneapi-ubuntu22-

      - name: Print CPU info and Setup Environment
        run: |
          CPU_NAME=$(lscpu | grep "Model name" | cut -d':' -f2 | xargs)
          echo "CPU_MODEL=$CPU_NAME" >> $GITHUB_ENV
          echo "NCORES=$(nproc)"     >> $GITHUB_ENV

      - name: Install system dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y \
            gcc g++ libgomp1 python3-pip \
            libdnnl-dev libbenchmark-dev \
            linux-tools-common linux-tools-generic
          if ! command -v perf &>/dev/null; then
            PERF_BIN=$(find /usr/lib/linux-tools -name perf 2>/dev/null | head -1)
            [ -n "$PERF_BIN" ] && sudo ln -sf "$PERF_BIN" /usr/local/bin/perf
          fi
          sudo sh -c 'echo 0 > /proc/sys/kernel/perf_event_paranoid'
          sudo sh -c 'echo 0 > /proc/sys/kernel/kptr_restrict'

      - name: Install Intel MKL
        if: steps.cache-mkl.outputs.cache-hit != 'true'
        run: |
          wget -qO /tmp/intel-gpg.pub \
            https://apt.repos.intel.com/intel-gpg-keys/GPG-PUB-KEY-INTEL-SW-PRODUCTS.PUB
          sudo gpg --dearmor -o /usr/share/keyrings/intel-oneapi.gpg /tmp/intel-gpg.pub
          echo "deb [signed-by=/usr/share/keyrings/intel-oneapi.gpg] \
            https://apt.repos.intel.com/oneapi all main" \
            | sudo tee /etc/apt/sources.list.d/oneAPI.list
          sudo apt-get update -qq
          sudo apt-get install -y intel-oneapi-mkl-devel

      - name: Capture oneAPI Environment
        shell: bash
        run: |
          SNAP_BEFORE=$(mktemp)
          env | sort > "$SNAP_BEFORE"

          CHILD_ENV=$(
            env -i \
              HOME="$HOME" \
              PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin" \
              TERM="${TERM:-dumb}" \
              bash --norc --noprofile -c '
                source /opt/intel/oneapi/setvars.sh --force >/dev/null 2>&1
                env
              '
          )
          CHILD_EXIT=$?
          if [ $CHILD_EXIT -ne 0 ]; then
            echo "ERROR: setvars.sh failed: $CHILD_EXIT"
            echo "$CHILD_ENV"
            exit $CHILD_EXIT
          fi

          SNAP_AFTER=$(mktemp)
          echo "$CHILD_ENV" | sort > "$SNAP_AFTER"

          comm -13 "$SNAP_BEFORE" "$SNAP_AFTER" | while IFS= read -r LINE; do
            KEY="${LINE%%=*}"
            VALUE="${LINE#*=}"
            case "$KEY" in
              BASH_*|BASHOPTS|SHELLOPTS|PPID|SHLVL|_|OLDPWD|TERM|HOME) continue ;;
              LD_LIBRARY_PATH|LIBRARY_PATH) continue ;;
            esac
            printf '%s<<%s\n%s\n%s\n' \
              "$KEY" "__ONEAPI_DELIM__" "$VALUE" "__ONEAPI_DELIM__" \
              >> "$GITHUB_ENV"
          done

          rm -f "$SNAP_BEFORE" "$SNAP_AFTER"

          MKL_ROOT_VAL=$(echo "$CHILD_ENV" | grep '^MKLROOT=' | cut -d= -f2-)
          MKL_SO=$(find "$MKL_ROOT_VAL" -name "libmkl_rt.so" 2>/dev/null | head -1)
          MKL_LIB=$(dirname "$MKL_SO")
          echo "MKL_LIB_PATH=$MKL_LIB" >> "$GITHUB_ENV"
          echo "SETVARS_COMPLETED=1"    >> "$GITHUB_ENV"
          echo "Capture done. MKLROOT=$MKL_ROOT_VAL  MKL_LIB=$MKL_LIB"

      - name: Install Python packages
        run: pip3 install matplotlib numpy pandas

      - name: Run autotuner
        run: |
          gcc autotuner/autotune.c -I. -O3 -march=core-avx2 -fopenmp -o autotune -lm
          ./autotune
          read BEST_MC BEST_NC BEST_KC BEST_PA1 BEST_PB1 BEST_PA2 BEST_PB2 \
            < /tmp/autotune_result.txt
          echo "BEST_MC=$BEST_MC"   >> $GITHUB_ENV
          echo "BEST_NC=$BEST_NC"   >> $GITHUB_ENV
          echo "BEST_KC=$BEST_KC"   >> $GITHUB_ENV
          echo "BEST_PA1=$BEST_PA1" >> $GITHUB_ENV
          echo "BEST_PB1=$BEST_PB1" >> $GITHUB_ENV
          echo "BEST_PA2=$BEST_PA2" >> $GITHUB_ENV
          echo "BEST_PB2=$BEST_PB2" >> $GITHUB_ENV

      # ── Compile + Run in ONE step ──────────────────────────────
      # Merging compile and run into a single step eliminates the
      # inter-step environment injection that was re-triggering
      # setvars.sh. Within one step, we control the shell completely.
      #
      # Linking strategy: static MKL via the Intel-provided
      # link-line helper script. Static linking means the binary
      # has ZERO runtime dependency on the oneAPI directory tree —
      # no dynamic linker event can spawn a shell that touches
      # setvars.sh. -lmkl_rt (dynamic) is replaced with the
      # full static group: -lmkl_intel_lp64 -lmkl_gnu_thread
      # -lmkl_core wrapped in --start-group/--end-group.
      # We also wrap the entire run in a clean env that strips
      # all oneAPI vars so even if something does spawn a shell,
      # there is nothing to trigger.
      - name: Compile and Run All Benchmarks
        shell: bash
        run: |
          set -euo pipefail

          MKL_LIB="$MKL_LIB_PATH"
          MKL_INC="$MKLROOT/include"

          echo "=== Verifying MKL static libs ==="
          ls "$MKL_LIB"/libmkl_intel_lp64.a \
             "$MKL_LIB"/libmkl_gnu_thread.a  \
             "$MKL_LIB"/libmkl_core.a

          DEFINES="-DMC=$BEST_MC -DNC=$BEST_NC -DKC=$BEST_KC \
            -DPREFETCH_A_L1=$BEST_PA1 -DPREFETCH_B_L1=$BEST_PB1 \
            -DPREFETCH_A_L2=$BEST_PA2 -DPREFETCH_B_L2=$BEST_PB2"

          # Static MKL link group — no dynamic MKL dep at all
          MKL_STATIC="-L$MKL_LIB \
            -Wl,--start-group \
              $MKL_LIB/libmkl_intel_lp64.a \
              $MKL_LIB/libmkl_gnu_thread.a  \
              $MKL_LIB/libmkl_core.a        \
            -Wl,--end-group"

          COMMON_FLAGS="-g -fopenmp -O3 -march=core-avx2 \
            $DEFINES -I$MKL_INC $MKL_STATIC -ldnnl -lm -lpthread"

          echo "=== Compiling standard kernel ==="
          gcc src/matmul.c $COMMON_FLAGS -o matmul

          echo "=== Compiling robust kernel ==="
          mv src/kernel.h src/kernel_std.h
          cp src/kernel_robust.h src/kernel.h
          gcc src/matmul.c $COMMON_FLAGS -o matmul_robust
          mv src/kernel_std.h src/kernel.h

          echo "=== Compiling Google Benchmark ==="
          g++ -O3 -march=core-avx2 -fopenmp \
            benchmarks/google_benchmark.cpp -o google_bench \
            $DEFINES -I./src -I$MKL_INC $MKL_STATIC \
            -lbenchmark -ldnnl -lpthread -lm

          echo "=== Running benchmarks in clean environment ==="
          # Strip ALL oneAPI vars from the runtime environment.
          # The static binary needs none of them. This is the
          # definitive fix — no oneAPI var in env = no trigger.
          env -i \
            HOME="$HOME" \
            PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin" \
            OMP_NUM_THREADS="$NCORES" \
            bash --norc --noprofile -c '
              set -euo pipefail
              ./matmul > standard_output.txt
              mv benchmark_results.csv standard_results.csv
              ./matmul_robust > robust_output.txt
              mv benchmark_results.csv robust_results.csv
              ./google_bench \
                --benchmark_out=google_results.json \
                --benchmark_out_format=json \
                --benchmark_filter="BM_MyKernel|BM_OneDNN|BM_ExtremeRange_Accuracy"
            '

      - name: Profile and Plot
        run: |
          # perf runs in a fresh env too — no oneAPI vars needed for profiling
          env -i \
            HOME="$HOME" \
            PATH="/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin" \
            bash --norc --noprofile -c '
              perf stat ./matmul > perf_report.txt 2>&1 || true
            '
          export CPU_MODEL="${{ env.CPU_MODEL }}"
          python3 scripts/plot.py
          python3 scripts/plot_google.py

      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: gemm-robust-results
          path: |
            standard_results.csv
            robust_results.csv
            google_results.json
            performance_plot.png
            google_bench_plot.png
            perf_report.txt
            standard_output.txt
            robust_output.txt